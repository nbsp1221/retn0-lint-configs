---
'@retn0/eslint-config': minor
'@retn0/oxlint-config': minor
---

Require curly braces for all control statements by enabling `curly` with the `"all"` option. Brace-less control statements now report an auto-fixable error.
